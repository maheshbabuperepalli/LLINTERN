<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-image: url("/public/score.png");
            background-repeat: no-repeat;
            background-size: cover;
            background-position: center;
            background-color: rgba(0, 0, 0, 0.402);
            font-family: Arial, sans-serif;
        }

        .container {
            text-align: center;
            border: 2px solid black;
            border-radius: 10px;
            box-shadow: 10px 10px 10px rgba(185, 201, 203, 0.737);
            background-color: #ffffffe0;
            padding: 20px;
            max-width: 40vw;
            margin: auto;
        }

        h1 {
            color: #003939;
        }

        h1 span {
            color: rgb(81, 0, 20);
        }

        /* New CSS for the button */
        #showAnswersBtn {
            margin-top: 20px; /* Add space between the button and the score */
            background-color: #007BFF;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
            font-weight: bold;
        }

        #showAnswersBtn:hover {
            background-color: #0056b3;
        }

        /* New CSS for the correctAnswersContainer */
        #correctAnswersContainer {
            margin-top: 20px; /* Add space between the container and the button */
            background-color: rgba(255, 255, 255, 0.8);
            padding: 10px;
            border-radius: 5px;
            display: none;
            font-weight: 900;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- score.ejs -->
        <h1>Your Score: <%= score %></h1>

        <!-- Wrap the button in a div -->
        <div class="button-container">
            <button id="showAnswersBtn">Show Correct Answers</button>
        </div>
        <div id="correctAnswersContainer" style="display:none;">
            <% for (let i = 0; i < correctnessArray.length; i++) { %>
                <p>
                Question <%= i + 1 %>: <%= quizData[i].question %><br>

                    <% if (correctnessArray[i]) { %>
                    Correct
                    <% } else { %>
                    Your Answer: <span style="color: red;"><%= participantAnswers[i] %></span> <br>
                   Correct Answer:  <span style="color: green;"><%= correctAnswers[i] %></span>
                    <% } %>
                </p>
            <% } %>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var showAnswersBtn = document.getElementById('showAnswersBtn');
            var correctAnswersContainer = document.getElementById('correctAnswersContainer');

            showAnswersBtn.addEventListener('click', function () {
                // Toggle the display style of the correct answers container
                correctAnswersContainer.style.display = (correctAnswersContainer.style.display === 'none') ? 'block' : 'none';
            });
        });
    </script>
</body>
</html>

